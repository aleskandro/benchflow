apiVersion: v1
kind: Secret
metadata:
  name: mlflow-s3-secret
  labels:
    app.kubernetes.io/name: benchflow
type: Opaque
stringData:
  access-key: "your-access-key-id"
  secret-key: "your-secret-access-key"
  bucket-name: "mlflow-artifacts"
  region: "us-east-1"
  archive-base-url: "https://your-s3-public.example.com/mlflow"
---
# MLflow S3 Credentials Secret
#
# This secret is required when MLflow is enabled and configured to store artifacts
# in S3-compatible storage (AWS S3, MinIO, Ceph, etc.).
#
# To create this secret:
#   1. Replace the placeholder values above with actual S3 credentials
#   2. Create the secret:
#      oc create -f config/secrets/mlflow-s3-creds.yaml
#
# Or create directly from command line:
#   oc create secret generic mlflow-s3-secret \
#     --from-literal=access-key=your-access-key-id \
#     --from-literal=secret-key=your-secret-access-key \
#     --from-literal=bucket-name=mlflow-artifacts \
#     --from-literal=region=us-east-1
#
# For Grafana archive replay:
#   archive-base-url should point to an HTTP(S) path that exposes the MLflow
#   artifact root and is reachable by Grafana Infinity. BenchFlow expects
#   archived metrics to be available under:
#     <archive-base-url>/<experiment_id>/<run_id>/artifacts/metrics/raw
#
# IMPORTANT: Never commit actual credentials to version control!
