apiVersion: benchflow.io/v1alpha1
kind: BenchmarkProfile
metadata:
  name: guidellm-smoke
spec:
  backend_type: openai_http
  rate_type: concurrent
  rates: [1]
  data: prompt_tokens=100,output_tokens=100
  max_seconds: 60
  env:
    GUIDELLM__REQUEST_TIMEOUT: "60"
