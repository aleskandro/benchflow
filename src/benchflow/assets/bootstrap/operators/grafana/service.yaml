apiVersion: v1
kind: Service
metadata:
  name: grafana
  namespace: "{{ GRAFANA_NAMESPACE }}"
  labels:
    app.kubernetes.io/name: benchflow
    benchflow.io/component: grafana
spec:
  selector:
    app.kubernetes.io/name: benchflow
    benchflow.io/component: grafana
  ports:
    - name: http
      port: 3000
      targetPort: http
